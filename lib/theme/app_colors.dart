import 'package:flutter/material.dart';

/// Steppe / khanate palette for the Ashina swipe game.
class AppColors {
  const AppColors._();

  // Base leathers and inks.
  static const ink = Color(0xFF0D0A06);
  static const leatherDark = Color(0xFF1A140C);
  static const leatherDeep = Color(0xFF120D08);
  static const leather = Color(0xFF2A2014);
  static const card = Color(0xFF221A10);

  // Gold accents.
  static const gold = Color(0xFFC9A24B);
  static const goldBright = Color(0xFFEEC36A);
  static const goldDim = Color(0xFF8A6B33);
  static const bronze = Color(0xFF7A5A2E);

  // Parchment / text tones.
  static const parchment = Color(0xFFF1E2BF);
  static const sand = Color(0xFFD8C09A);
  static const stone = Color(0xFF9A9384);

  // Semantic.
  static const success = Color(0xFF6FBF73);
  static const info = Color(0xFF64A8DC);
  static const danger = Color(0xFFD96B4A);
  static const amber = Color(0xFFD6A23F);
}
